# Flash sweep traces

This folder is reserved for sanitized flash candidate sweep artifacts used by docs.

If sweep outputs are produced, place sanitized files here and link from
`docs/android-legacy.md` evidence sections.
